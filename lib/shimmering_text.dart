/// Animated shimmer effects for text and arbitrary widgets.
///
/// Exposes two widgets:
///
/// * [ShimmeringText] — a [Text]-like widget with a shimmer sweep, ideal for
///   loading / streaming labels.
/// * [Shimmer] — wraps any child with the same animated gradient sweep.
library shimmering_text;

export 'src/shimmer.dart';
export 'src/shimmering_text.dart';
