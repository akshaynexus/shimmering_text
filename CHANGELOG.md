## 0.1.0

- Initial release.
- `ShimmeringText` — a `Text`-like widget with an animated shimmer sweep.
- `Shimmer` — wraps any widget with the same gradient sweep.
- Configurable base/highlight colors, duration, gradient stops, and an
  `enabled` toggle.
